# Chrome Releases

This folder is reserved for Chrome auto-update artifacts.

The update manifest is [updates.xml](updates.xml).
The signed package should be published as `radio-stream-chrome-v0.2.crx`.
